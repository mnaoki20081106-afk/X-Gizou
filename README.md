# X-Gizou

X-Gizou is an iOS multi-profile WebKit browser prototype built with SwiftUI. It isolates website data per profile and supports explicit User-Agent presets for compatibility testing.

> This project does not bypass platform attestation, device-bound security controls, or account enforcement. “Device profile” values are UI/testing presets, not a claim of hardware identity.
